<%= form_with(model: chirp, local: true) do |form| %>
  <% if chirp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chirp.errors.count, "error") %> prohibited this chirp from being saved:</h2>

      <ul>
        <% chirp.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.text_field :chirp, id: :chirp_chirp, class: "form-control" %>
  </div>
  <div class="custom-file">
    <%= form.file_field :photo, id: :chirp_photo,  placeholder: "Please choose a photo or video" %>
    <%= form.label :photo, class: "custom-file-label", for: "chirp_photo" %>
  </div>
  <div style="padding-top: 6px;">
    <%= form.submit class:"btn btn-pink btn-block" %>
  </div>
<% end %>
